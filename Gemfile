# frozen_string_literal: true

source "https://rubygems.org"

# Agent Orchestra has zero runtime dependencies.
# It uses only Ruby stdlib: yaml, fileutils, time, securerandom, open3, net/http, json.

group :development, :test do
  gem "minitest", "~> 5.0"
  gem "rake", "~> 13.0"
end
